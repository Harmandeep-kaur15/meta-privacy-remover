const fs=require('fs'); const sharp=require('sharp'); const ExifParser=require('exif-parser'); const buffer=fs.readFileSync('test.png'); (async ()=>{ try{ const metadata=await sharp(buffer).metadata(); console.log('sharp metadata', metadata); } catch(err){ console.error('sharp error', err); } try{ const parser=ExifParser.create(buffer); const result=parser.parse(); console.log('exif tags', result.tags); } catch(err){ console.error('exif parse error', err); } })();